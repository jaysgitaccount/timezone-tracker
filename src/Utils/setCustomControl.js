
// If I want to import this and set the controls up OUTSIDE of the component body
// factory function maybe? with get/set?
// let keyboardEnter
// how I'd like to structure this is to have a helper function that is imported and then the key + callback occurs
// not sure if having a separate thing is necessary though

// onKeyDown={ }
function setCustomControl(key, ) {
    
}

export default setCustomControl;